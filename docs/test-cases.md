# 测试用例设计

## 1. 用户认证模块

### 1.1 注册功能

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| AUTH-001 | 正常注册 | `{"username":"user1","password":"password123"}` | 返回 200，包含用户信息和 token | P0 |
| AUTH-002 | 用户名已存在 | `{"username":"user1","password":"password123"}`（重复注册） | 返回 400，提示"用户名已存在" | P1 |
| AUTH-003 | 用户名为空 | `{"username":"","password":"password123"}` | 返回 400，提示"用户名不能为空" | P1 |
| AUTH-004 | 密码为空 | `{"username":"user2","password":""}` | 返回 400，提示"密码不能为空" | P1 |
| AUTH-005 | 密码长度不足 | `{"username":"user2","password":"123"}` | 返回 400，提示"密码长度不足" | P1 |

### 1.2 登录功能

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| AUTH-006 | 正常登录 | `{"username":"user1","password":"password123"}` | 返回 200，包含 token | P0 |
| AUTH-007 | 用户名不存在 | `{"username":"nonexist","password":"password123"}` | 返回 401，提示"用户名或密码错误" | P1 |
| AUTH-008 | 密码错误 | `{"username":"user1","password":"wrongpassword"}` | 返回 401，提示"用户名或密码错误" | P1 |
| AUTH-009 | 空用户名 | `{"username":"","password":"password123"}` | 返回 400，提示"用户名不能为空" | P1 |
| AUTH-010 | 空密码 | `{"username":"user1","password":""}` | 返回 400，提示"密码不能为空" | P1 |

### 1.3 JWT 验证

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| AUTH-011 | 携带有效 token 访问受保护接口 | 有效 JWT | 返回 200 | P0 |
| AUTH-012 | 未携带 token 访问受保护接口 | 无 Authorization 头 | 返回 401，提示"未认证" | P0 |
| AUTH-013 | 携带无效 token | 伪造的 JWT | 返回 401，提示"未认证" | P1 |
| AUTH-014 | 携带过期 token | 过期的 JWT | 返回 401，提示"未认证" | P1 |

### 1.4 权限隔离

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| AUTH-015 | 普通用户访问管理员接口 | USER 角色 JWT | 返回 403，提示"无权限" | P0 |
| AUTH-016 | 管理员访问普通用户接口 | ADMIN 角色 JWT | 返回 200 | P1 |
| AUTH-017 | 普通用户访问其他用户会话 | USER 角色 JWT + 其他用户 sessionId | 返回 403 或数据为空 | P1 |

## 2. 风险检测模块

### 2.1 高风险词识别

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| RISK-001 | 包含"想死" | "我不想活了" | `isHighRisk()` 返回 `true` | P0 |
| RISK-002 | 包含"自杀" | "我想自杀" | `isHighRisk()` 返回 `true` | P0 |
| RISK-003 | 包含"割腕" | "我想割腕" | `isHighRisk()` 返回 `true` | P0 |
| RISK-004 | 包含"跳楼" | "我想跳楼" | `isHighRisk()` 返回 `true` | P0 |
| RISK-005 | 普通焦虑表达 | "最近压力很大，有点焦虑" | `isHighRisk()` 返回 `false` | P0 |
| RISK-006 | 日常聊天 | "今天天气真好" | `isHighRisk()` 返回 `false` | P1 |
| RISK-007 | 空消息 | "" | `isHighRisk()` 返回 `false` | P1 |
| RISK-008 | null 消息 | null | `isHighRisk()` 返回 `false` | P1 |

### 2.2 危机响应

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| RISK-009 | 危机回复包含热线 | 调用 `crisisReply()` | 返回内容包含"12356" | P0 |
| RISK-010 | 危机回复包含急救电话 | 调用 `crisisReply()` | 返回内容包含"120" | P0 |
| RISK-011 | 危机回复包含报警电话 | 调用 `crisisReply()` | 返回内容包含"110" | P0 |
| RISK-012 | 获取帮助资源列表 | 调用 `resources()` | 返回 3 条资源记录 | P1 |

### 2.3 危机工作流

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| RISK-013 | 高风险消息触发告警 | 高风险消息 | 创建 RiskAlertEvent 记录 | P0 |
| RISK-014 | 告警事件进入队列 | 高风险消息 | 事件发布到 Redis Stream | P1 |
| RISK-015 | 重复相同消息不重复告警 | 相同高风险消息（短时间内） | 只创建一条告警记录 | P1 |

## 3. 智能聊天模块

### 3.1 意图分类

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| CHAT-001 | 普通聊天意图 | "你好，今天过得怎么样" | 分类为 `CHAT` | P1 |
| CHAT-002 | 心理咨询意图 | "我最近总是失眠，该怎么办" | 分类为 `CONSULT` | P1 |
| CHAT-003 | 风险意图 | "我不想活了" | 分类为 `RISK` | P0 |

### 3.2 普通聊天

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| CHAT-004 | 正常聊天回复 | "你好" | 返回友好回复 | P1 |
| CHAT-005 | 空消息 | "" | 返回错误提示 | P1 |
| CHAT-006 | 聊天记录保存 | 发送消息 | 消息保存到数据库 | P1 |

### 3.3 RAG 咨询

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| CHAT-007 | 知识库有相关内容 | "焦虑的原因是什么" | 返回基于知识库的回答 | P1 |
| CHAT-008 | 知识库无相关内容 | "量子物理是什么" | 返回"知识库信息有限" | P1 |
| CHAT-009 | 引用来源标注 | "怎么缓解焦虑" | 回答中包含来源说明 | P1 |

## 4. 工具调用模块

### 4.1 工具决策

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| TOOL-001 | 需要搜索知识库 | "焦虑的原因是什么" | 决策调用 `search_knowledge` | P1 |
| TOOL-002 | 需要获取仪表盘 | "我的报告情况" | 决策调用 `get_dashboard` | P1 |
| TOOL-003 | 需要获取用户记忆 | "你还记得我吗" | 决策调用 `get_user_memory` | P1 |
| TOOL-004 | 需要风险扫描 | "帮我分析这段文字的风险" | 决策调用 `risk_scan` | P1 |
| TOOL-005 | 需要策略推荐 | "我最近压力很大，给我一些建议" | 决策调用 `recommend_strategy` | P1 |
| TOOL-006 | 普通聊天不需要工具 | "你好" | `needTool` 为 `false` | P1 |

### 4.2 工具执行

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| TOOL-007 | 工具执行成功 | 有效工具和参数 | 返回执行结果 | P1 |
| TOOL-008 | 缺少必填参数 | 缺少 query 参数 | 返回错误提示 | P1 |
| TOOL-009 | 参数类型错误 | 字符串传入数字参数 | 返回类型错误提示 | P1 |
| TOOL-010 | 工具不存在 | 不存在的工具名 | 返回错误提示 | P1 |

## 5. 记忆系统模块

### 5.1 会话管理

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| MEM-001 | 创建新会话 | 首次消息 | 创建新会话记录 | P1 |
| MEM-002 | 恢复历史会话 | 已有 sessionId | 加载历史会话 | P1 |
| MEM-003 | 会话标题生成 | 消息内容 | 根据消息生成标题（≤24字） | P1 |

### 5.2 短期记忆

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| MEM-004 | 最近12条消息缓存 | 发送13条消息 | Redis 只保留最近12条 | P1 |
| MEM-005 | 缓存过期 | 超过24小时 | Redis 缓存自动过期 | P2 |
| MEM-006 | Redis 故障降级 | Redis 不可用 | 从 MySQL 读取历史消息 | P1 |

### 5.3 中长期记忆与画像

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| MEM-007 | 会话摘要更新 | 多轮对话 | 会话摘要更新 | P1 |
| MEM-008 | 中期记忆更新 | 跨会话对话 | 中期记忆更新 | P1 |
| MEM-009 | 长期记忆更新 | 稳定信息输入 | 长期记忆更新 | P1 |
| MEM-010 | 用户画像更新 | 用户偏好表达 | 画像字段更新 | P1 |
| MEM-011 | LLM 更新失败降级 | LLM 调用失败 | 使用简单拼接方式更新 | P1 |

## 6. 知识库模块

### 6.1 文档导入

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| KNOW-001 | 导入文本文件 | 文本内容 | 文档保存到数据库 | P1 |
| KNOW-002 | 文档分块 | 长文本 | 文本被分块处理 | P1 |
| KNOW-003 | Embedding 生成 | 文本内容 | 生成向量并保存到 Chroma | P1 |
| KNOW-004 | 重复导入 | 相同文档 | 不重复插入 | P1 |

### 6.2 知识检索

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| KNOW-005 | 精确匹配检索 | "焦虑" | 返回焦虑相关知识 | P1 |
| KNOW-006 | 模糊匹配检索 | "压力大" | 返回相关知识 | P1 |
| KNOW-007 | 分类过滤检索 | 焦虑分类 | 只返回焦虑类知识 | P1 |
| KNOW-008 | 无结果检索 | "不存在的关键词" | 返回空列表 | P1 |

### 6.3 重排

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| KNOW-009 | 结果重排 | 检索结果 | 结果按相关性排序 | P1 |
| KNOW-010 | 重排服务降级 | 重排服务不可用 | 使用 fallback 策略 | P1 |

## 7. 管理员模块

### 7.1 记忆中心

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| ADMIN-001 | 查看所有用户记忆 | ADMIN JWT | 返回所有用户记忆列表 | P1 |
| ADMIN-002 | 查看单个用户记忆 | ADMIN JWT + username | 返回指定用户记忆 | P1 |
| ADMIN-003 | 查看用户会话消息 | ADMIN JWT + username + sessionId | 返回会话消息列表 | P1 |
| ADMIN-004 | 普通用户访问记忆中心 | USER JWT | 返回 403 | P0 |

### 7.2 报告管理

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| ADMIN-005 | 获取所有报告 | ADMIN JWT | 返回所有报告列表 | P1 |
| ADMIN-006 | 分页获取报告 | ADMIN JWT + page + size | 返回分页结果 | P1 |
| ADMIN-007 | 获取单个报告 | ADMIN JWT + id | 返回报告详情 | P1 |
| ADMIN-008 | 获取仪表盘数据 | ADMIN JWT | 返回全局统计数据 | P1 |
| ADMIN-009 | 获取高风险用户 | ADMIN JWT | 返回高风险用户列表 | P1 |

### 7.3 用户管理

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| ADMIN-010 | 获取用户列表 | ADMIN JWT | 返回所有用户 | P1 |
| ADMIN-011 | 获取用户详情 | ADMIN JWT + username | 返回用户信息和画像 | P1 |

## 8. 留言反馈模块

### 8.1 用户提交反馈

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| FEED-001 | 正常提交反馈 | 反馈内容 | 创建反馈记录，状态为 PENDING | P1 |
| FEED-002 | 空内容提交 | "" | 返回错误提示 | P1 |
| FEED-003 | 超长内容提交 | 超过 2000 字 | 内容被截断或返回错误 | P1 |

### 8.2 管理员处理反馈

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| FEED-004 | 查看所有反馈 | ADMIN JWT | 返回反馈列表 | P1 |
| FEED-005 | 查看待处理反馈 | ADMIN JWT | 返回 PENDING 状态反馈 | P1 |
| FEED-006 | 回复反馈 | ADMIN JWT + reply | 反馈状态变为 REPLIED | P1 |
| FEED-007 | 关闭反馈 | ADMIN JWT | 反馈状态变为 CLOSED | P1 |

### 8.3 用户查看反馈

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| FEED-008 | 查看自己的反馈 | USER JWT | 返回该用户的反馈列表 | P1 |
| FEED-009 | 查看他人反馈 | USER JWT + 他人反馈ID | 返回 403 或空 | P1 |

## 9. 健康检查模块

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| HEALTH-001 | 健康检查（匿名） | GET /actuator/health | 返回 UP | P0 |
| HEALTH-002 | 信息检查（匿名） | GET /actuator/info | 返回应用信息 | P1 |
| HEALTH-003 | 指标检查（管理员） | ADMIN JWT + GET /actuator/metrics | 返回指标数据 | P1 |
| HEALTH-004 | 指标检查（普通用户） | USER JWT + GET /actuator/metrics | 返回 403 | P1 |

## 10. 限流模块

| 用例ID | 测试场景 | 输入 | 预期结果 | 优先级 |
|--------|----------|------|----------|--------|
| RATE-001 | 正常请求（未超限） | 在限制范围内请求 | 返回正常响应 | P1 |
| RATE-002 | 请求超限 | 短时间内大量请求 | 返回 429 或限流错误 | P1 |

## 附录：测试用例执行模板

```bash
# 示例：运行风险检测单元测试
cd backend
./mvnw test -Dtest=RiskDetectionServiceTests

# 示例：接口测试（登录）
curl -X POST http://localhost:8080/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","password":"test123456"}'

# 示例：接口测试（聊天，需先获取 token）
TOKEN="your-jwt-token"
curl -X POST http://localhost:8080/chat \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"message":"你好"}'
```